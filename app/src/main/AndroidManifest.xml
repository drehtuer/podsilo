<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Active downloads run as a foreground service so Doze and background limits can't stall a
         half-finished episode (CLAUDE.md §11). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".PodsiloApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WorkManager declares SystemForegroundService with NO foregroundServiceType, and from
             API 34 the type passed to startForeground() must be a subset of the manifest's. Without
             this merge, DownloadWorker's setForeground(... FOREGROUND_SERVICE_TYPE_DATA_SYNC)
             throws IllegalArgumentException *inside the system's service dispatch*, which is a hard
             crash of the whole process, not a caught failure:

                 foregroundServiceType 0x00000001 is not a subset of
                 foregroundServiceType attribute 0x00000000 in service element of manifest file

             The FOREGROUND_SERVICE_DATA_SYNC permission above is necessary and not sufficient — the
             service element needs the type too. Found on the first real download on a Pixel 5
             (Android 14); the emulator run never reached a download, which is why 500-odd green
             tests said nothing about it. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- WorkManager's own startup initializer would build a default Configuration with the
             no-arg WorkerFactory, which cannot construct our @HiltWorkers. Removing it defers
             initialisation to PodsiloApplication's Configuration.Provider. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
