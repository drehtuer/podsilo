# Robolectric 4.15.1 supports up to SDK 35; pin the tests to a supported level rather than the
# module's compileSdk/targetSdk (37), which Robolectric can't yet instrument. Same pin as
# :core:database. Headless, no emulator — still Tier 1/2 in spirit (CLAUDE.md §4).
sdk=34
