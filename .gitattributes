# Auto-detect text files and normalise to LF, except the Windows wrapper script.
* text=auto eol=lf

*.bat text eol=crlf

# Binary — never diff or normalise.
gradle/wrapper/gradle-wrapper.jar binary
*.jar binary
*.png binary
*.jpg binary
