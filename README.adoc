= Podsilo
:idprefix:
:idseparator: -

// The icon form, not either lockup: the lockups carry live Archivo `<text>`, and `UI.adoc` §C2
// requires that to be outlined before the asset leaves the app — README is named in that rule, and
// Archivo is not in this repository. The icon also carries its own accent ground (§C5 allows the
// mark on the accent field), so it stays legible on GitHub's light and dark themes alike, which a
// two-colour mark on transparency would not.
//
// Raw HTML rather than `image::...[align="center"]`, and that is not a preference. Asciidoctor
// centres a block image by wrapping it in `<div class="imageblock text-center">`, which needs
// Asciidoctor's own stylesheet — GitHub loads none of it and strips the wrapper outright (checked
// against GitHub's rendered output: zero occurrences of `text-center`). `<p align="center">`
// survives its sanitiser and is what actually centres the mark on the page people read.
++++
<p align="center">
  <img src="assets/logos/podsilo-icon.svg" width="96" alt="Podsilo">
</p>
++++

image:https://github.com/drehtuer/podsiloApp/actions/workflows/ci.yml/badge.svg?branch=main[CI,link=https://github.com/drehtuer/podsiloApp/actions/workflows/ci.yml]
image:https://img.shields.io/badge/dynamic/json?url=https%3A%2F%2Fsonarcloud.io%2Fapi%2Fmeasures%2Fcomponent%3Fcomponent%3Ddrehtuer_podsiloApp%26metricKeys%3Dline_coverage&query=%24.component.measures%5B0%5D.value&suffix=%25&label=lines&color=4c1[Line coverage,link=https://sonarcloud.io/component_measures?id=drehtuer_podsiloApp&metric=line_coverage]
image:https://img.shields.io/badge/dynamic/json?url=https%3A%2F%2Fsonarcloud.io%2Fapi%2Fmeasures%2Fcomponent%3Fcomponent%3Ddrehtuer_podsiloApp%26metricKeys%3Dbranch_coverage&query=%24.component.measures%5B0%5D.value&suffix=%25&label=branches&color=4c1[Branch coverage,link=https://sonarcloud.io/component_measures?id=drehtuer_podsiloApp&metric=branch_coverage]
image:https://github.com/drehtuer/podsiloApp/actions/workflows/github-code-scanning/codeql/badge.svg?branch=main[CodeQL,link=https://github.com/drehtuer/podsiloApp/security/code-scanning]
image:https://github.com/drehtuer/podsiloApp/actions/workflows/docs.yml/badge.svg?branch=main[Docs,link=https://drehtuer.github.io/podsiloApp]
image:https://img.shields.io/github/v/release/drehtuer/podsiloApp?sort=semver[Release,link=https://github.com/drehtuer/podsiloApp/releases/latest]
link:LICENSE[image:https://img.shields.io/badge/licence-GPL--3.0--or--later-blue[Licence: GPL-3.0-or-later\]]

An Android *podcast catcher* that downloads episodes into a folder you control -- and deliberately
does not play them.

Think of it as a silo: episodes flow in from your feeds, pool in a folder you picked, and are
consumed by whatever audio player you actually like.

== The idea

Podsilo follows the podcast subscriptions stored in a self-hosted *Nextcloud* instance (via the
https://github.com/thrillfall/nextcloud-gpodder[gpoddersync] app) and lists their episodes for
manual triage. For each new episode you choose *download* or *skip* -- nothing is ever downloaded
automatically, and nothing is downloaded twice.

That last part is the app's central job. Every download emits a `DOWNLOAD` action and every skip
emits a `PLAY` action back to the server, so an episode you have handled here never shows up as new
again -- not on this device, and not in RePod or AntennaPod on another one.

Downloaded files are renamed and re-tagged to a configurable template
(`Der Podcast/20260714_Warum-Hamburg-immer-regnet.mp3`), because the filenames in someone else's
player are the entire user experience of a download. Feeds are inconsistent about this; Podsilo
isn't.

== What it is not

* *Not a player.* No playback, no playlists, no speed control.
* *Not a feed manager.* The subscription list is read-only -- you add and remove feeds in Nextcloud.
There is no add-feed UI and never will be.
* *Not automatic.* Nothing is ever downloaded that you didn't ask for. No auto-download rules, no
background triage, nothing queued by a sync or a refresh. You can tell it to fetch a whole
podcast's worth of undecided episodes in one go -- that's a command you issue, not a rule that runs
behind you. Sync works the same way: there is no periodic pass, so state moves between the phone
and Nextcloud when you pull to refresh, press _Sync now_, or make a decision -- never on a timer.
* *Not a file manager.* Once a file lands in your folder it belongs to you and your player.
Podsilo does not delete it, track it, or care whether it still exists.

Personal project, single user, no cloud service, no telemetry, no ads.

== Technologies

Kotlin, Jetpack Compose and Material 3 on the surface; Room, WorkManager, DataStore, Hilt and
Retrofit/OkHttp underneath. Feeds are parsed with
https://github.com/prof18/RSS-Parser[rssparser], audio tags rewritten with the Android-compatible
https://github.com/Kaned1as/jaudiotagger[jaudiotagger fork], and downloads written through the
Storage Access Framework so you can point them at an SD card. Both library choices deviate from the
first pick, and link:docs/modules/ROOT/pages/architecture.adoc[`docs/architecture.adoc`] §7 and §11 say why -- as does the
decision to add no date-time library at all.

The codebase is split into small modules (`:core:model`, `:core:feed`, `:core:naming`,
`:core:download`, `:core:gpodder`, `:core:sync`, `:feature:*`) so the interesting logic -- sync
reconciliation and filename generation above all -- is plain-JVM testable without an emulator.

A guiding rule: *use existing libraries, don't invent replacements.* No hand-rolled XML parser, no
hand-written ID3 frames, no bespoke retry logic.

== Development

Development happens in a Docker dev container (JDK 17 + Android SDK) on Windows/WSL2. Tests come in
three tiers: JVM unit tests as the default and main workload, a headless emulator in the container
for instrumented runs, and a real device -- over USB or wireless debugging -- driven from the container
for everything a device has to answer for. Sync is tested against a disposable
https://codeberg.org/kd2/opodsync[opodsync] server, never against a real Nextcloud.

Setup instructions are in link:docs/modules/ROOT/pages/dev-environment.adoc[`docs/dev-environment.adoc`], which is also honest
about which tiers have actually been run -- all three have, and its status table says what each run
proved and what is still unverified. The module design, schema and sync semantics are in
link:docs/modules/ROOT/pages/architecture.adoc[`docs/architecture.adoc`]; the whole of the UI -- the screen design, the Compose
seam it binds to, and the brand mark -- is in link:docs/modules/ROOT/pages/UI.adoc[`docs/UI.adoc`].

Five documents, and that is deliberate: `architecture.adoc` and `UI.adoc` are the two references,
link:docs/modules/ROOT/pages/backlog.adoc[`backlog.adoc`] holds what is not being built,
link:docs/modules/ROOT/pages/third-party.adoc[`third-party.adoc`] tracks licences, and
link:docs/modules/ROOT/pages/journal.adoc[`journal.adoc`] keeps a running log -- this project doubles
as an experiment in agent-driven development, so the process is recorded alongside the result.
link:docs/modules/ROOT/pages/decisions/[`decisions/`] keeps only the decisions that still constrain
the code and cannot be re-derived from the two references; everything else was folded into the
section that governs it.

They are an https://antora.org/[Antora] component, which is why the paths above are
`docs/modules/ROOT/pages/` rather than plain `docs/` -- `antora.yml` and `modules/` have to be
siblings. `./scripts/build-docs.sh` builds the site into `build/site/`. One consequence
worth knowing before clicking a link in here: pages reference each other with Antora's `xref:` macro,
which GitHub's file view does not resolve, so cross-references work on the built site and not while
browsing the repository. That site is published at
https://drehtuer.github.io/podsiloApp[drehtuer.github.io/podsiloApp] on every push to `main` that touches
the documentation, and it is the better place to read any of this: the links resolve and the 26
Mermaid diagrams are rendered.

Two further files sit outside `docs/` because they are addressed to the agent rather than to a
reader: link:.claude/CLAUDE.adoc[`.claude/CLAUDE.adoc`] is the standing brief every session starts
from -- scope, the non-goals, the mandated libraries, the definition of done -- and
link:.claude/skills/run-podsilo/SKILL.md[`.claude/skills/run-podsilo/SKILL.md`] documents the driver
that boots the headless emulator, builds and installs the app, seeds it with podcasts, and taps
through it by on-screen text.

The brief is AsciiDoc like everything else, which puts it under the same `asciidoctor
--failure-level=WARN` check CI runs over every `.adoc`. Claude Code only loads a file named
`CLAUDE.md`, so link:.claude/CLAUDE.md[`.claude/CLAUDE.md`] remains as a stub that imports it with
`@CLAUDE.adoc`. Section references written as "CLAUDE.md §4" -- in the documents and in code
comments -- mean §4 of the AsciiDoc file.

== Licence

GPL-3.0-or-later.
